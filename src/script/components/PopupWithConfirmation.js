import Popup from "./PopUp.js";

export default class PopupWithConfirmation extends Popup {
  constructor(popupDeleteCard) {
    super(popupDeleteCard);
  }
  
  setEventListeners() {
    super.setEventListeners();
  }
  
  open() {
    super.open();
  }
  
  close() {
    super.close();
  }
}